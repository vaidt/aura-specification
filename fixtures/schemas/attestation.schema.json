{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://github.com/AuraIDToken/aura-specification/fixtures/schemas/attestation.schema.json",
  "title": "APS-200 ENT-006 Attestation",
  "_status": "DRAFT CONTRACT — top-level structure is closed and current working-pack linkage is concrete; governance and approval lifecycle remain version-bound work outside the structural schema.",
  "description": "Draft machine-readable contract for an Attestation. Governance and approval lifecycle remain version-bound work outside the structural schema.",
  "type": "object",
  "required": [
    "object_id",
    "object_type",
    "protocol_version",
    "schema_version",
    "created_at",
    "integrity_hash",
    "attestation_type",
    "attested_execution_id",
    "evidence_reference",
    "attestation_hash"
  ],
  "properties": {
    "object_id": {
      "type": "string",
      "minLength": 1
    },
    "object_type": { "const": "Attestation" },
    "protocol_version": {
      "type": "string",
      "minLength": 1
    },
    "schema_version": {
      "type": "string",
      "minLength": 1
    },
    "created_at": {
      "type": "string",
      "format": "date-time"
    },
    "integrity_hash": {
      "type": "string",
      "pattern": "^[a-f0-9]{64}$"
    },
    "attestation_type": {
      "type": "string",
      "minLength": 1
    },
    "attested_execution_id": {
      "type": "string",
      "minLength": 1
    },
    "evidence_reference": {
      "type": "string",
      "minLength": 1
    },
    "attestation_hash": {
      "type": "string",
      "pattern": "^[a-f0-9]{64}$"
    }
  },
  "unevaluatedProperties": false
}
