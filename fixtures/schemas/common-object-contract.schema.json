{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://github.com/AuraIDToken/aura-specification/fixtures/schemas/common-object-contract.schema.json",
  "title": "APS-200 Common Object Contract",
  "_status": "DRAFT TOP-LEVEL STRUCTURAL CONTRACT — semantic/profile closure remains governed by APS text and open decision records.",
  "description": "Draft machine-readable contract for the common APS-200 object envelope required on every entity.",
  "type": "object",
  "required": [
    "object_id",
    "object_type",
    "protocol_version",
    "schema_version",
    "created_at",
    "integrity_hash"
  ],
  "properties": {
    "object_id": {
      "type": "string",
      "minLength": 1,
      "description": "Canonical identifier. Exact syntax remains bound to the applicable entity/profile contract."
    },
    "object_type": {
      "type": "string",
      "enum": [
        "ProtocolHeader",
        "EvaluationRequest",
        "EvaluationResult",
        "PolicyReference",
        "Evidence",
        "Attestation",
        "AuditRecord",
        "ImplementationMetadata"
      ]
    },
    "protocol_version": {
      "type": "string",
      "minLength": 1,
      "description": "Normative protocol contract version."
    },
    "schema_version": {
      "type": "string",
      "minLength": 1,
      "description": "Representation/schema contract version for the entity."
    },
    "created_at": {
      "type": "string",
      "format": "date-time",
      "description": "UTC ISO 8601 creation timestamp."
    },
    "integrity_hash": {
      "type": "string",
      "pattern": "^[a-f0-9]{64}$",
      "description": "Lowercase hexadecimal SHA-256 digest of the canonical object, excluding the integrity_hash member itself."
    }
  },
  "unevaluatedProperties": false
}
